{
    "version": "0.2.0",
    "configurations": [
      {
        "type": "node",
        "request": "launch",
        "name": "Launch oclif pcfg_tool debugger",
        "program": "${workspaceFolder}/bin/run",
        "args": [
          "parse",
          "${workspaceFolder}/corpus/cky_example.rules",
          "${workspaceFolder}/corpus/cky_example.lexicon",
          "book the flight through Houston"
        ]
      }
    ]
  }